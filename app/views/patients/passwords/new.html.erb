<div class="container mt-5">
  <div class="row">
    <div class="col-md-10 offset-3">
      <h5 class="mt-5 mr-0 mb-3 fas fa-key">パスワード再発行</h5><br/>
      <h6 class="mb-3">入力したメールアドレスに再設定用のURLが送られます</h6>
      <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
      <%= render 'layouts/error_messages', model: f.object %>
        <table class="table-sm ml-5">
          <tbody class="mt-3">
            <tr>
              <td>メールアドレス</td>
              <td><%= f.email_field :email, autofocus: true, autocomplete: "email", size: "40x1" %></td>
            </tr>
            <tr>
              <td></td>
              <td><%= f.submit "URLを取得する", class: "btn btn-primary mt-3" %></td>
            </tr>
          </tbody>
        </table>
      <% end %>
      <h6 class="mt-5">会員登録がお済みでない方</h6>
      <p class="ml-5"><%= link_to 'こちら', new_patient_registration_path, class: "fas fa-lock" %>　から新規登録を行ってください。</p>
    </div>
  </div>
</div>